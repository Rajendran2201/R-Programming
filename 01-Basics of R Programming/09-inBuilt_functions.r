df <- data.frame(Fruit_name=c("apple","banana","orange"), Fruit_cost=c(100,50,20))
print(str(df))    # Gives the structure of the data frame 

# head() - view first 5 rows of the dataframe 
# tail() - view last 5 rows of the dataframe 

# table() - identify the frequency of a label in the categorical column in a table 
# min(), max(), mean(), range() Eg : max(iris $ petal_length)